# Richal - A-Packages：包结构组织

## ✅ 当前包结构（最低要求已满足）

当前所有类都在 `richal` 包中，符合最低要求。

```
src/main/java/
└── richal/                    [source root: src/main/java]
    ├── Richal.java           # 主类
    ├── Ui.java               # 用户界面
    ├── Parser.java           # 命令解析器
    ├── Storage.java          # 文件存储
    ├── TaskList.java         # 任务列表
    ├── Task.java             # 任务基类
    ├── Todo.java             # Todo 任务
    ├── Deadline.java         # Deadline 任务
    ├── Event.java            # Event 任务
    ├── DateTimeParser.java   # 日期时间解析工具
    └── DukeException.java    # 自定义异常
```

**包声明：** 所有类都声明为 `package richal;` ✅

这个结构：
- ✅ 符合最低要求：所有类在一个包 `richal` 中
- ✅ `src/main/java` 保持为源根文件夹
- ✅ 没有错误地将 `src`、`main`、`java` 转换为包名

## 🚀 扩展目标：多包组织（可选）

随着类数量增加，可以将类组织到多个子包中：

### 建议的包结构

```
src/main/java/
└── richal/
    ├── Richal.java                    # 主类（保持在根包）
    │
    ├── task/                          # 任务相关类
    │   ├── Task.java
    │   ├── Todo.java
    │   ├── Deadline.java
    │   ├── Event.java
    │   └── TaskList.java
    │
    ├── command/                       # 命令相关类
    │   └── Parser.java
    │
    ├── ui/                            # 用户界面类
    │   └── Ui.java
    │
    ├── storage/                       # 存储相关类
    │   └── Storage.java
    │
    ├── util/                          # 工具类
    │   └── DateTimeParser.java
    │
    └── exception/                     # 异常类
        └── DukeException.java
```

### 包的职责划分

| 包名 | 职责 | 包含的类 |
|------|------|----------|
| `richal` | 主包，包含应用程序入口 | `Richal.java` |
| `richal.task` | 任务领域模型 | `Task`, `Todo`, `Deadline`, `Event`, `TaskList` |
| `richal.command` | 命令解析和处理 | `Parser` |
| `richal.ui` | 用户界面交互 | `Ui` |
| `richal.storage` | 数据持久化 | `Storage` |
| `richal.util` | 工具类 | `DateTimeParser` |
| `richal.exception` | 异常定义 | `DukeException` |

## 📝 如何重组包结构

如果你想采用多包结构，需要进行以下步骤：

### 步骤 1：创建子包目录

```bash
cd src/main/java/richal
mkdir task command ui storage util exception
```

### 步骤 2：移动文件到对应包

```bash
# 移动任务相关类
mv Task.java Todo.java Deadline.java Event.java TaskList.java task/

# 移动命令类
mv Parser.java command/

# 移动 UI 类
mv Ui.java ui/

# 移动存储类
mv Storage.java storage/

# 移动工具类
mv DateTimeParser.java util/

# 移动异常类
mv DukeException.java exception/
```

### 步骤 3：更新包声明

每个移动的文件需要更新包声明，例如：

**Task.java** (在 `task/` 目录中)
```java
package richal.task;  // 原来是 package richal;
```

**Parser.java** (在 `command/` 目录中)
```java
package richal.command;
```

**Ui.java** (在 `ui/` 目录中)
```java
package richal.ui;
```

等等...

### 步骤 4：更新 import 语句

所有引用这些类的文件都需要更新 import 语句。

例如，`Richal.java` 需要添加：
```java
package richal;

import richal.task.Task;
import richal.task.TaskList;
import richal.ui.Ui;
import richal.storage.Storage;
import richal.exception.DukeException;

public class Richal {
    // ...
}
```

## 🎯 当前建议

**对于当前项目规模（11个类）：**

✅ **保持当前结构**（所有类在 `richal` 包中）

**原因：**
1. 项目规模较小，单一包已经足够清晰
2. 满足最低要求
3. 减少维护成本
4. import 语句更简单

**何时考虑多包结构：**
- 类数量超过 15-20 个
- 添加更多功能模块（如数据库、网络等）
- 团队协作需要明确的模块边界
- 需要更好的代码组织和封装

## 📊 对比总结

### 单包结构（当前）
✅ 优点：
- 简单直接
- 无需复杂的 import
- 适合小型项目
- 符合最低要求

❌ 缺点：
- 所有类在同一层级
- 难以一眼识别类的类别

### 多包结构（扩展）
✅ 优点：
- 清晰的模块划分
- 更好的封装
- 易于找到相关类
- 适合大型项目

❌ 缺点：
- 需要更多的 import 语句
- 目录结构更复杂
- 对于小项目可能过度设计

## 🏁 结论

**当前状态：** ✅ 已满足 A-Packages 的最低要求

你的代码结构完全符合要求！所有类都正确地组织在 `richal` 包中，`src/main/java` 作为源根文件夹。

如果你想进一步优化（扩展目标），可以考虑将类分成多个子包，但对于当前的项目规模，单一包结构已经足够好了。
