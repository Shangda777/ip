# Richal - A-MoreOOP：面向对象重构

## 🎯 完成的重构

已成功将代码重构为更加面向对象的设计，提取了以下类：

### 1. **Ui 类** ✅
处理所有与用户的交互。

**职责：**
- 显示欢迎消息
- 显示再见消息
- 显示任务列表
- 显示错误消息
- 读取用户输入
- 显示任务操作的反馈

**主要方法：**
- `showWelcome()` - 显示欢迎消息
- `showGoodbye()` - 显示再见消息
- `showTaskList(TaskList)` - 显示任务列表
- `showError(String)` - 显示错误消息
- `readCommand()` - 读取用户命令
- `showTaskAdded()` / `showTaskMarked()` / `showTaskDeleted()` - 显示操作反馈

### 2. **Parser 类** ✅
负责解析用户命令并执行相应的操作。

**职责：**
- 解析用户输入
- 识别命令类型（list, mark, unmark, delete, todo, deadline, event, bye）
- 执行相应的操作
- 处理命令参数
- 调用 Storage 保存数据

**主要方法：**
- `parse(String, TaskList, Ui, Storage)` - 解析并执行命令
- `parseIndex(String, int)` - 解析任务索引
- `saveToStorage()` - 保存到存储

### 3. **Storage 类** ✅
（已存在）处理从文件加载任务和保存任务到文件。

**职责：**
- 从文件加载任务
- 保存任务到文件
- 创建数据目录和文件
- 解析文件格式

### 4. **TaskList 类** ✅
（已存在）包含任务列表，有添加/删除任务的操作。

**职责：**
- 管理任务数组
- 添加任务
- 删除任务
- 获取任务
- 获取任务数量

### 5. **Richal 主类** ✅
（重构后）变得更加简洁，只负责协调各个组件。

**职责：**
- 初始化各个组件（Ui, Storage, TaskList）
- 运行主循环
- 协调各个组件的交互

## 📊 重构前后对比

### 重构前的 Richal 类
```java
public class Richal {
    public static void main(String[] args) {
        // 168 lines of code
        // - Handles user input/output
        // - Parses commands
        // - Manages tasks
        // - Saves to storage
        // All in one place!
    }
}
```

### 重构后的 Richal 类
```java
public class Richal {
    private Storage storage;
    private TaskList taskList;
    private Ui ui;
    
    public Richal(String filePath) {
        ui = new Ui();
        storage = new Storage(filePath);
        // Load tasks...
    }
    
    public void run() {
        ui.showWelcome();
        while (!isExit) {
            String input = ui.readCommand();
            isExit = Parser.parse(input, taskList, ui, storage);
        }
    }
    
    public static void main(String[] args) {
        new Richal(FILE_PATH).run();
    }
}
```

## 🏗️ 架构设计

```
┌─────────────────────────────────────────┐
│              Richal (Main)              │
│  - Coordinates all components          │
│  - Runs main loop                       │
└──────────┬──────────────────────────────┘
           │
           ├──────────────┐
           │              │
           ▼              ▼
    ┌──────────┐    ┌──────────┐
    │    Ui    │    │  Parser  │
    │          │    │          │
    └──────────┘    └──────────┘
           │              │
           │              ▼
           │        ┌──────────┐
           │        │ TaskList │
           │        │          │
           │        └──────────┘
           │              │
           └──────┬───────┘
                  ▼
            ┌──────────┐
            │ Storage  │
            │          │
            └──────────┘
```

## ✨ OOP 设计优势

### 1. **单一职责原则（Single Responsibility Principle）**
每个类只负责一件事：
- `Ui` 只负责用户交互
- `Parser` 只负责解析命令
- `Storage` 只负责文件操作
- `TaskList` 只负责管理任务

### 2. **高内聚低耦合**
- 每个类的方法都高度相关
- 类之间通过清晰的接口交互
- 修改一个类不会影响其他类

### 3. **易于测试**
- 每个类可以独立测试
- 可以轻松 mock 其他组件

### 4. **易于扩展**
- 添加新命令只需修改 `Parser`
- 修改 UI 不影响业务逻辑
- 更改存储格式只需修改 `Storage`

### 5. **代码可读性**
- 主类 `Richal` 非常简洁，一眼看清程序流程
- 每个类的职责明确
- 方法名清晰表达意图

## 📝 代码组织

### 项目结构
```
src/main/java/richal/
├── Richal.java          # 主类，协调所有组件
├── Ui.java              # 用户界面
├── Parser.java          # 命令解析器
├── Storage.java         # 文件存储
├── TaskList.java        # 任务列表管理
├── Task.java            # 任务基类
├── Todo.java            # Todo 任务
├── Deadline.java        # Deadline 任务
├── Event.java           # Event 任务
├── DateTimeParser.java  # 日期时间解析
└── DukeException.java   # 自定义异常
```

## 🔄 重构步骤

按照建议的方式，一步步进行重构：

1. ✅ **步骤 1**：创建 `Ui` 类
   - 提取所有 `System.out.println()` 到 Ui 类
   - 测试确保正常工作

2. ✅ **步骤 2**：创建 `Parser` 类
   - 提取命令解析逻辑到 Parser 类
   - 测试确保所有命令正常工作

3. ✅ **步骤 3**：重构 `Richal` 主类
   - 简化 main 方法
   - 使用 Ui 和 Parser
   - 测试整体功能

4. ✅ **步骤 4**：验证
   - 运行所有测试
   - 确保功能没有改变
   - 检查代码质量

## 🧪 测试验证

所有功能测试通过：
- ✅ 添加任务（todo, deadline, event）
- ✅ 列出任务
- ✅ 标记任务
- ✅ 取消标记任务
- ✅ 删除任务
- ✅ 数据持久化
- ✅ 错误处理

## 💡 未来可能的扩展（Stretch Goal）

可以进一步提取 Command 类：

```
Command (abstract)
├── AddCommand
│   ├── TodoCommand
│   ├── DeadlineCommand
│   └── EventCommand
├── DeleteCommand
├── ListCommand
├── MarkCommand
├── UnmarkCommand
└── ExitCommand
```

这样主循环可以简化为：
```java
Command c = Parser.parse(input);
c.execute(tasks, ui, storage);
```

## 📌 关键改进

1. **代码行数减少**：主类从 186 行减少到 64 行
2. **清晰的职责分离**：每个类都有明确的单一职责
3. **更好的可维护性**：修改更容易，bug 更少
4. **遵循 OOP 原则**：封装、单一职责、高内聚低耦合

---

**重构完成！** 🎉

代码现在更加面向对象，更易于理解、测试和扩展。
